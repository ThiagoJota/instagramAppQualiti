<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:background="@color/cinza"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context="br.gov.pe.recife.sauderecife.view.activity.DetalheHospitalActivity">

    <ImageView
        android:id="@+id/imagem"
        android:layout_width="320dp"
        android:layout_height="320dp"
        android:layout_centerHorizontal="true" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        tools:text="Usuario Comentário"
        android:id="@+id/usuarioComentario"
        android:textSize="20sp"
        android:textColor="@android:color/black"
        android:layout_below="@+id/imagem"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="10dp" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        tools:text="Usuario Comentário"
        android:id="@+id/ultimoComentario"
        android:textSize="20sp"
        android:textColor="@android:color/black"
        android:layout_below="@+id/usuarioComentario"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="10dp" />

</RelativeLayout>
